services:
  - type: web
    name: socs-exam-hub
    env: python
    buildCommand: cd frontend_src && npm install && npm run build && cd .. && pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --chdir backend app:app
